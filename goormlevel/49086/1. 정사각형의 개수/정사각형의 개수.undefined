# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
length = int(input())
sum = 0
for i in range(1, length):
	#개수? i가 1부터 길이-1까지 반복
	#i가1이면 걍 i**i
	#i가 2면 length-i+1??
	sum += (length - i + 1)**2
	
print(sum+1)